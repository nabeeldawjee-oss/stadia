{
  "name": "@stadia/db",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "build": "prisma db push && tsup src/index.ts --format cjs --out-dir dist --no-splitting",
    "build:vercel": "prisma generate && tsup src/index.ts --format cjs --out-dir dist --no-splitting",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "ts-node": "^10.9.2",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0"
  }
}
